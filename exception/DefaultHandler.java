public class DefaultHandler {

    public static void main(String[] args) {
        
/*      If user does not specify the suitable exception,
        Java will throw the exception to the default exception handler. */
        String value = null;
        System.out.println(value.length());

    }
}